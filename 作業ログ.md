# 作業ログ

## 2026-07-17 リポジトリ初期構築・システム構成把握・取込まわりの改修
- 実施内容:
  - 【環境構築】引き継ぎ文書に基づき作業環境を構築。GitHub private リポジトリ
    access-vba-work を作成し、c:\project\access-vba-work に clone、
    初期構成（README / CLAUDE.md / .gitignore / docs / ログ類 / src）を作成して push
  - 【構成把握】客先 Access システム（WORCS）の全体構成をヒアリングし
    docs/システム構成.md に記録。WORCS_データ（テーブル本体）／WORCS_取込（基幹
    データの取込）／WORCS_管理（統計・ユーザー管理、中身はまだ無い）／
    WORCS_利用者（利用者処理）の4ファイル分割。
    流れ: 基幹出力 → 取込 → データに蓄積 → 利用者で処理
  - 【エラー70修正】WORCS_利用者を開いていると取込時のバックアップ作成
    （FileCopy srcDB, backupFile）がエラー70になる問題を診断・修正。
    原因は FileCopy がコピー元を排他で開く仕様。FSO.CopyFile への置き換え＋
    コピー後に DAO で開いて検証する modBackup.bas を作成 → 客先で動作確認OK
  - 【複数Excel一括取込】取込仕様（Excel → テンポラリ → 反映クエリで本番へ追加）
    に対し、週明け等の複数ファイルをまとめて取り込む modImportBatch.bas を作成。
    当初はファイル単位で反映まで行うループ案だったが、「反映は既存クエリで行うので
    テンポラリに複数ファイル分を積み上げるだけでよい」との指示で方式変更。
    最終形: FileDialog 複数選択 → テンポラリを最初に1回だけ空に →
    全ファイルを TransferSpreadsheet で順に追記 → 反映は既存処理に任せる
  - 【運用ルール変更】作業ログを1日1エントリ方式＋年次アーカイブに変更
    （大本の Dropbox\CLAUDE.md を更新。~/.claude/CLAUDE.md はそこへのシンボリックリンクと確認）
  - 【取込結果の行数表示】完了・中断メッセージを「ファイル数＋合計行数」の併記に変更
    （TransferSpreadsheet 前後の DCount 差分で行数を取得）。基幹側の出力件数との
    突き合わせ検算ができるように
  - 【push 自動化】このリポジトリに限り、指示なしで commit & push まで自動実行して
    よいとの承認を得た（プロジェクト CLAUDE.md に記録。他プロジェクトには適用しない）
  - 【運用反映の重複ダイアログ問題】inbox/運用反映.bas（客先の現行コード・inbox
    運用の初利用）を確認。反映ボタンの重複チェックで、登録済み1件ごとに MsgBox を
    出すため、1000件登録済みだと1000回ダイアログが出る問題。
    修正: 重複は黙ってスキップして件数を集計し、最後に「対象/追加/スキップ件数＋
    内訳先頭20件」を1回だけ表示する src/運用反映_修正版.bas を作成（モジュール全体
    差し替え）。コメントアウトされていた On Error GoTo Err_Handler も有効化
  - 【登録日・更新日の仕様変更】登録日・更新日を Now()（時刻付き）から Date
    （日付のみ）に変更する方針をユーザーが決定（クエリで = 比較が使えて楽になるため。
    当初は登録日のみの予定だったが更新日も Date に統一）。
    運用反映_修正版.bas を両方 Date に修正し、既存データの時刻を一括除去する
    1回きりの処理 src/一括修正_時刻除去.bas を作成（登録日・更新日それぞれの
    件数確認→確認ダイアログ→UPDATE。実行前バックアップを促す）。
    日付フィルタの範囲条件は新旧どちらのデータでも動くのでそのまま
  - 【利用者側・日付フィルタ】F_特定5講座（メイン）＋F_SUB_特定5講座（一覧）＋
    F_SUB_特定5講座_チェック状況（日次処理用）の画面で、全件表示 → 登録日=今日を
    初期表示にし、ドロップダウンで別日付に切替できるようにする改修。
    案B（データがある登録日だけを DISTINCT で一覧化したコンボボックス）を採用し、
    src/F_特定5講座_日付フィルタ.bas を作成。当初は VBA の Filter 方式（時刻付き
    登録日に対応する範囲条件）だったが、登録日・更新日を Date に統一したことを受けて
    「コンボ参照方式」に作り直し: クエリの抽出条件に
    [Forms]![F_特定5講座]![cbo対象日] を設定し、VBA は初期値（今日）設定と
    Requery のみのシンプル版。クエリに Date() を直書きすると日付切替が壊れる
    （クエリで絞った後にフィルタがかかるため他の日が常に0件になる）点を説明済み。
    コンボ追加・クエリ抽出条件などデザインビューでの準備手順はファイル冒頭に記載。
    客先での動作確認時に「指定されたフィールド『登録日』が FROM 句にある複数の
    テーブルを参照しました」エラーが発生 → DATE_SOURCE に結合クエリを指定した
    ことによる曖昧参照が原因。DATE_SOURCE は T_WORCS 直接指定に修正して解消
  - 【逆方向の受け渡し】客先→こちらのコード受け渡し用に inbox/ フォルダを新設。
    客先PCのブラウザで GitHub の Add file → Create new file に VBE のコードを
    貼り付けてコミットする運用（手順は inbox/README.md に記載、客先PCでも読める）。
    パスワード・接続文字列は貼る前に <<プレースホルダ>> へ置換する注意も明記
- 作成/変更ファイル:
  - README.md、CLAUDE.md、.gitignore、docs/決定事項.md、作業ログ.md、現状まとめ.md（新規・初期構築）
  - docs/システム構成.md（新規）
  - src/modBackup.bas（新規・動作確認済み）
  - src/modImportBatch.bas（新規・客先確認待ち）
  - C:\Users\vse50\Dropbox\CLAUDE.md（更新・ログ運用ルール）
- 決定・結論:
  - 運用方式は docs/決定事項.md の通り（GitHub private 経由のコピペ運用）
  - エラー70対応は案A（FSO置き換え）を採用。コピー元本体への影響はゼロ、
    リスクは書き込み瞬間と重なった場合のバックアップ不整合のみで、検証処理で検知可能
  - 一括取込は「テンポラリ積み上げ方式」を採用（反映・バックアップは既存処理側で
    実施するため本モジュールには含めない）。エラー時はそのファイルで停止
    （取込済み分はテンポラリに残る）。取込済みフォルダへのファイル移動（MoveToDone）は
    不要との指示で削除
- 動作確認: 本日作成分（modBackup / modImportBatch / 運用反映_修正版 /
  一括修正_時刻除去 / F_特定5講座_日付フィルタ）はすべて客先で動作確認OK
- 次にやること:
  - 客先の許可（業務コードを GitHub private に置くことの確認）が未確認なら確認
  - 未確認事項の収集: テーブル接続方式・基幹出力の形式・Office の bit 数
  - 運用反映の重複チェックが遅くなってきたら FindFirst → 複合インデックス＋Seek 化を検討

## 2026-07-21 債権管理課の追加（2部署体制へ）
- 実施内容:
  - 対象部署を2部署に拡大。既存分＝サービスセンター、新規＝債権管理課と整理
  - 同一リポジトリ＋部署別フォルダ方式（案A）を採用。src/債権管理課/ と
    inbox/債権管理課/ を新設。既存のサービスセンター分は src/ 直下のまま動かさない
    （客先で参照済みのパスを壊さないため）
  - README / CLAUDE.md / inbox/README.md / docs/システム構成.md を2部署体制の
    記載に更新（inbox の手順にはフォルダ付きファイル名の入力方法を追記）
  - 反映済みの inbox/運用反映.bas を削除（成果は src/運用反映_修正版.bas に反映済み・
    動作確認OK。Git 履歴には残る）
- 作成/変更ファイル:
  - src/債権管理課/.gitkeep、inbox/債権管理課/.gitkeep（新規）
  - README.md、CLAUDE.md、inbox/README.md、docs/システム構成.md（更新）
  - inbox/運用反映.bas（削除）
- 決定・結論:
  - 別リポジトリ案（案B）と比較し、閲覧者が本人のみである前提で案Aを採用。
    債権管理課の担当者にもブラウザで直接見せる運用になったら案Bへの分割を再検討
- 実施内容（追記）:
  - フォルダ名を「SC／債権管理課」の対称構成に変更。既存の VBA 5ファイルを
    src/ 直下から src/SC/ へ git mv で移動し、inbox/SC/ を新設。
    README / CLAUDE.md / inbox/README.md / 現状まとめのパス記載も更新
  - git commit / push が auto モードの自動判定でブロックされた件の恒久対応として、
    .claude/settings.json を新設し git add/commit/push 等の許可ルールを登録
    （これまでは明示ルールなしで毎回クラシファイア判定だった）
- 実施内容（追記・債権管理課の初タスク）:
  - 【SMS配信の一括処理化】SMS配信画面（顧客抽出→Excel＋配信用CSV出力）を
    一括処理画面に組み込む改修を開始。inbox 経由で現行コード2本を受領
    （SMS配信.bas 390行・一括処理.bas 734行）
  - 抽出条件の事前保存方式を設計: T_SMS配信抽出条件（ID=1 の1レコード運用、
    残高From/To・最終取次日/入金約束日の日数と未入力フラグ・受託日From/To・不到達）。
    客先でテーブル作成済み。条件設定画面は連結フォーム方式
    （src/債権管理課/F_SMS抽出条件.bas に更新日自動セット等を作成）
  - 共通モジュール src/債権管理課/mod一括SMS抽出.bas を作成:
    抽出＋Excel/CSV出力を画面から切り離し、LogMsg 方式で
    一括時=txtLogへ追記／単体時=MsgBox を切替。ループ内 MsgBox のログ化、
    Option Explicit 追加（totalRow バグ）、外部委託ステータス空文字時の
    INSERT 未クローズバグ修正、Null/空文字の扱い統一を実施
  - 組み込みコード src/債権管理課/一括処理_SMS組込.bas を作成
    （chkSMS配信 チェックボックス＋SMS配信一括処理 Sub、出力先は \DATA）
  - T_SMS配信抽出条件 に「送信テキスト」列の追加が必要と判明（客先作業）
- 決定・結論:
  - コピペ複製ではなく共通モジュール化（案B）を採用。既存 SMS配信画面は
    当面そのまま残し、共通版の動作確認後に画面側も共通関数呼び出しへ置換予定
  - btn配信済み（配信結果の書き戻し）は一括化の対象外（手作業のまま）
- 次にやること:
  - 客先で: 送信テキスト列の追加 → モジュール貼り付け → chkSMS配信 追加 →
    単体テスト → 一括実行テスト
  - 動作確認後: SMS配信画面のボタンを共通関数呼び出しに置き換える改修
  - 債権管理課の構成メモを docs/ に作成（ヒアリング続き）

## 2026-07-22 フォルダ名を SK に変更
- 実施内容:
  - 債権管理課のフォルダ名を SC に合わせた略称 SK に変更
    （src/債権管理課 → src/SK、inbox/債権管理課 → inbox/SK、git mv で履歴維持）
  - README / CLAUDE.md / inbox/README.md / 現状まとめ の表記を SK（債権管理課）に更新
- 作成/変更ファイル: 上記リネームと各ドキュメント（更新）
- 次にやること: 前日分と同じ（SK の客先テスト待ち。送信テキスト列追加 →
  モジュール貼り付け → chkSMS配信 追加 → 単体・一括テスト）
