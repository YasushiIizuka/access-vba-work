# 現状まとめ

**最終更新日**: 2026-07-21

## 全体像
- 客先の Access VBA 開発を、GitHub private リポジトリ経由のコピペ運用で支援する環境
- **対象は2部署**（2026-07-21 に債権管理課を追加）:
  - SC（サービスセンター・既存分・WORCS）: src/SC/・inbox/SC/
  - 債権管理課（新規）: src/債権管理課/・inbox/債権管理課/。初タスク進行中（下記）
- こちら→客先: Claude Code がコードを生成して src/ に push → 客先ブラウザ（github.com）でコピー → Access VBE に貼り付け
- 客先→こちら: 客先の現行コードを VBE からコピー → github.com のブラウザ編集で inbox/ に保存 → こちらで pull（手順は inbox/README.md）
- この往復運用は実際に機能することを確認済み（inbox 経由で運用反映.bas を受領し、修正版を客先で動作確認するまで一巡した）
- サービスセンターのシステム（WORCS）は4ファイル分割構成: WORCS_データ（テーブル本体）／WORCS_取込（基幹データ取込）／WORCS_管理（統計・ユーザー管理、中身はまだ無い）／WORCS_利用者（利用者処理）。詳細は docs/システム構成.md 参照

## 基本方針・判断軸
- 機密情報はコードに書かない（プレースホルダ方式）。inbox に貼る際もパスワード等は置換
- Access バイナリはコミットしない（テキストのみ管理）
- 1モジュール = 1ファイル、コピペ単位で完結させる
- PC 間の同期は GitHub の push / pull で行う（Dropbox 等の同期フォルダには置かない）
- このリポジトリに限り push は指示不要で自動実行してよい（2026-07-17 承認）
- T_WORCS の登録日・更新日は Date（日付のみ）で統一（2026-07-17 仕様決定）

## 完了したタスク（SC・すべて客先で動作確認OK・2026-07-17）
- エラー70修正: src/SC/modBackup.bas（FileCopy → FSO.CopyFile ＋ DAO 検証）
- 複数 Excel の一括取込: src/SC/modImportBatch.bas（複数選択 → テンポラリ積み上げ、
  ファイル数＋合計行数を表示）
- 運用反映の重複ダイアログ連発修正: src/SC/運用反映_修正版.bas（サマリー表示方式）
- 登録日・更新日の Date 統一: src/SC/一括修正_時刻除去.bas（既存データの時刻除去、実行済み）
- F_特定5講座 の日付フィルタ: src/SC/F_特定5講座_日付フィルタ.bas（コンボ参照方式、
  初期表示=今日、データがある日だけ一覧・新しい順）

## 進行中: 債権管理課・SMS配信の一括処理化（2026-07-21 着手）
- 内容: SMS配信画面（顧客抽出→Excel＋配信用CSV）を一括処理画面（txtLog ログ方式）に組み込む
- 方式: 抽出条件は T_SMS配信抽出条件（ID=1・1レコード運用）に事前保存。
  ロジックは共通モジュール化し、LogMsg で一括時=ログ／単体時=MsgBox を切替
- 納品済み（客先確認待ち）: src/債権管理課/ の F_SMS抽出条件.bas・
  mod一括SMS抽出.bas・一括処理_SMS組込.bas
- 客先の残作業: T_SMS配信抽出条件 に「送信テキスト」列追加 → 貼り付け →
  chkSMS配信 追加 → 単体・一括テスト
- 確認後の次改修: SMS配信画面のボタンを共通関数呼び出しへ置換（ロジック一本化）
- 対象外: btn配信済み（配信結果の書き戻し）は手作業のまま残す

## 論点・未決事項
- 客先の許可（業務コードを外部サービスに置くことの契約・情報管理規程上の確認）が未確認
- 債権管理課のシステム内容が未ヒアリング（Access の構成・困りごと・Office の bit 数）
- 同一リポジトリ運用のため、閲覧者が本人以外に広がる場合はリポジトリ分割を再検討
  （GitHub のアクセス権はリポジトリ単位のため）
- WORCS_管理 は統計・ユーザー管理の予定でまだ中身がない（今後の開発対象になる見込み）
- 運用反映の重複チェックは1行ごとの FindFirst のため、T_WORCS が大きく育つと遅くなる
  （遅くなったら複合インデックス＋Seek 化を検討）

## 次アクション
1. 債権管理課のヒアリング（システム構成・相談内容）→ docs/ に構成メモを作成
2. サービスセンター: 次の開発タスクの指示を待つ（候補: WORCS_管理の統計・ユーザー管理）
3. 客先の許可の確認（未済なら）
4. 未確認事項の収集: テーブル接続方式・基幹出力のファイル形式・Office の bit 数
