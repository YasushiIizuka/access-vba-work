# 現状まとめ

**最終更新日**: 2026-07-17

## 全体像
- 客先の Access VBA 開発を、GitHub private リポジトリ経由のコピペ運用で支援する環境
- 自宅・事務所で Claude Code がコードを生成して push → 客先ブラウザ（github.com）でコピー → Access VBE に貼り付け
- リポジトリ初期構築は完了（2026-07-17）。実際の VBA 開発タスクはまだ未着手
- 客先システム（WORCS）は4ファイル分割構成: WORCS_データ（テーブル本体）／WORCS_取込（基幹データ取込）／WORCS_管理／WORCS_利用者（利用者処理）。詳細は docs/システム構成.md 参照

## 基本方針・判断軸
- 機密情報はコードに書かない（プレースホルダ方式）
- Access バイナリはコミットしない（テキストのみ管理）
- 1モジュール = 1ファイル、コピペ単位で完結させる
- PC 間の同期は GitHub の push / pull で行う（Dropbox 等の同期フォルダには置かない）

## 論点・未決事項
- 客先の許可（業務コードを外部サービスに置くことの契約・情報管理規程上の確認）が未確認
- 客先ブラウザで github.com にログインできるかが未確認（不可ならこの方式は成立しないため代替案を再検討）
- 客先ブラウザで claude.ai が開けるか（開けるなら往復不要の運用に発展できる）

## 進行中のタスク
- WORCS_取込 のバックアップ処理エラー（エラー70）修正: FileCopy を FSO.CopyFile に
  置き換える src/modBackup.bas を作成済み。push → 客先での貼り付け・動作確認待ち

## 次アクション
1. src/modBackup.bas を push し、客先で WORCS_取込 に貼り付けて動作確認
   （既存の FileCopy 行を SafeCopyDatabase 呼び出しに差し替え）
2. 次回の客先訪問時に動作確認チェックリストを実施（README 参照）
